"use client";
import Link from "next/link"; import { usePathname } from "next/navigation"; import { BarChart3, CalendarDays, Image, LayoutDashboard, Lightbulb, TrendingUp } from "lucide-react"; import { cn } from "@/lib/utils";
const ITEMS = [{ href: "/marketing", label: "Visão Geral", icon: LayoutDashboard, exact: true },{ href: "/marketing/calendario", label: "Calendário", icon: CalendarDays },{ href: "/marketing/ideias", label: "Banco de Ideias", icon: Lightbulb },{ href: "/marketing/relatorios", label: "Relatórios", icon: BarChart3 },{ href: "/marketing/assets", label: "Assets", icon: Image },{ href: "/marketing/trafego", label: "Tráfego Pago", icon: TrendingUp }];
export function MarketingSubNav() { const pathname = usePathname(); return <div className="border-b px-4 pt-3 sm:px-6 sm:pt-4" style={{ borderColor: "var(--border)" }}><div className="-mx-4 scroll-px-4 overflow-x-auto overscroll-x-contain px-4 [scrollbar-width:none] sm:mx-0 sm:px-0"><nav className="flex min-w-max gap-1">{ITEMS.map((item) => { const active = item.exact ? pathname === item.href : pathname.startsWith(item.href); const Icon = item.icon; return <Link key={item.href} href={item.href} className={cn("flex min-h-11 items-center gap-1.5 px-3 pb-2 pt-1 text-sm font-medium transition-colors sm:min-h-0 sm:pb-3 sm:pt-0", active ? "text-[var(--text-title)]" : "text-[var(--muted-foreground)] hover:text-[var(--text-title)]")} style={{ borderBottom: active ? "2px solid var(--primary)" : "2px solid transparent" }}><Icon size={14}/>{item.label}</Link>; })}</nav></div></div>; }
