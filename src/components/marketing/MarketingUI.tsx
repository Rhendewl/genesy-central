"use client";
import { Inbox } from "lucide-react"; import { CONTENT_STATUS_LABELS, type MarketingContentStatus } from "@/types/marketing";
const COLORS: Record<MarketingContentStatus, string> = { planned: "#7d99ad", in_production: "#e0a344", in_review: "#9b7fe0", approved: "#6b9b6f", scheduled: "#4a8fd4", published: "#22c55e", cancelled: "#7c878e" };
export function ContentStatusBadge({ status }: { status: MarketingContentStatus }) { const color = COLORS[status]; return <span className="inline-flex items-center gap-1 rounded-full px-2 py-0.5 text-[10px] font-medium" style={{ color, background: `${color}18`, border: `1px solid ${color}35` }}><span className="h-1.5 w-1.5 rounded-full" style={{ background: color }}/>{CONTENT_STATUS_LABELS[status]}</span>; }
export function MarketingEmptyState({ title, description, action }: { title: string; description: string; action?: React.ReactNode }) { return <div className="flex min-h-56 flex-col items-center justify-center gap-2 rounded-2xl p-8 text-center" style={{ border: "1px dashed var(--glass-border)", background: "var(--glass-bg-soft)" }}><Inbox size={26} style={{ color: "var(--muted-foreground)" }}/><p className="text-sm font-medium" style={{ color: "var(--text-title)" }}>{title}</p><p className="max-w-sm text-xs" style={{ color: "var(--muted-foreground)" }}>{description}</p>{action && <div className="mt-2">{action}</div>}</div>; }
export function MarketingSkeleton() { return <div className="grid animate-pulse gap-4 md:grid-cols-3">{Array.from({ length: 6 }).map((_, i) => <div key={i} className="h-32 rounded-2xl" style={{ background: "var(--glass-bg-soft)" }}/>)}</div>; }
